export enum FormType {
    Decision = 'decision',
    Batch = 'batch',
}

export enum ApiEndpoint {
    Decisions = 'decisions',
    Batches = 'batches',
    Users = 'users',
}

export enum Up2TomResponseType {
    Success = 'success',
    Error = 'error',
}